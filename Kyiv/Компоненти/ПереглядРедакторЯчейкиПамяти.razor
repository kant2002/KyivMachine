@inject МашинаКиїв Машина

<span title="@Конвертер.ДоДвойноїТочності(Машина.Память[Адреса]).ToString("F12")">@ПарсерКоманд.Сконвертувати(Машина.Память[Адреса])</span>

@code {
    [Parameter]
    public int Адреса { get; set; }
}